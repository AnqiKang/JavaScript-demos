const dAge = 20;

if (dAge > 20) {
    console.log("You are over 20 years old! ")
}

const dNames = ['shaun', 'mario', 'luigi', 'karen'];

if (dNames.length > 3) {
    console.log("Thta's a lot of ninjas! ");
}

const dPassword = "passwordissonglong";
if (dPassword.length >= 12) {
    console.log("thta password is might strong! ")
}else if (dPassword.length >= 8) {
    console.log("That password is long enough!");
}else {
    console.log("That password is not long enough!");
}
