let myAge1;

// the first value is undefined because we have not given it a value yet.
// the second value is NaN. 
// the third value is the age is undefined
console.log(myAge1, myAge1 + 3, `the age is ${myAge1}`)



let myAge2 = null;
// the first value is null
// the second value is 3. 
// the third value is the age is null
console.log(myAge2, myAge2 + 3, `the age is ${myAge2}`)


